#include "main.h"
#include <string.h>

/**
* _strlen - function that returns the lenght of a string
* @s: string of words
*
* Return: length of the string
*/

int _strlen(char *s)
{
int len;

len = strlen(s);
return (len);
}
